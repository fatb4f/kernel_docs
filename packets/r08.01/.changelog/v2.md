All three findings are correct. I generated a reconciled `r08.01` packet that fixes them.

Files:

* [r08.01.final.packet.v2.md](sandbox:/mnt/data/r08.01.final.packet.v2.md)
* [r08.01.final.packet.v2.json](sandbox:/mnt/data/r08.01.final.packet.v2.json)

What changed:

1. **Single canonical manifest contract**

   * The packet now declares one authoritative field set only:

     * `manifest_id`, `spec_version`, `owner_plane`, `inputs`, `outputs`, `admission`, `regen`
   * It also includes a **legacy-field translation matrix** and explicitly forbids the old top-level fields:

     * `id`, `input_refs`, `output_class_ids`, `consuming_plane`, `admissibility_requirement`, `handler_id`, `regen_policy`
   * It adds a **packet-set consistency matrix** requiring `manifests.md` to be updated or removed if it still carries the legacy shape.

2. **Single path basis**

   * All normative paths are now explicitly **repo-relative** and must begin with `kernel/`.
   * Manifest paths must stay under `kernel/manifests/`.
   * Output paths must stay under `kernel/generated/`.
   * The surface-index fragment and structured-output fragment now use the same basis.

3. **Explicit example migration**

   * The packet now states that the old family-level examples are being replaced by class-specific examples.
   * It defines the replacement mapping.
   * It requires `kernel/examples/valid/example.index.json` and `kernel/examples/invalid/example.index.json` to remove legacy entries in the same change, so there is only one authoritative example registry.

This `v2` should be treated as the superseding `r08.01` packet.
